/** Max results shown (spec §UI) and the API browse fetch limit. */
export const RESULT_LIMIT = 50;
/** Quiet period before a keystroke promotes `inputValue` to the search `query`. */
export const DEBOUNCE_MS = 150;
