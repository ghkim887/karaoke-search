{
  "name": "karaoke-search",
  "private": true,
  "version": "0.0.1",
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "lint": "biome check .",
    "format": "biome format --write .",
    "build": "corepack pnpm -r build",
    "test": "corepack pnpm -r test",
    "typecheck": "corepack pnpm --filter @karaoke/schema build && corepack pnpm --filter @karaoke/search build && corepack pnpm -r typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "vitest": "^2.1.8"
  }
}
