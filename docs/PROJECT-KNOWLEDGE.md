# Project Knowledge

Technical invariants and past failures, refreshed against code at `4e37fa1`
on 2026-09-17; references consolidated 2026-09-18. Structural overview is in
[ARCHITECTURE.md](ARCHITECTURE.md);
remaining work is in [ROADMAP.md](ROADMAP.md#open-questions).

A historical fact relevant to the data model: the song **category dimension**
(`jpop`/`vocaloid`/`anime`) was removed repo-wide — the app is search-only
(vendor filter only); the empty-state landing's J-POP/Vocaloid/Anime section
labels are cosmetic UI strings, not a schema field.

## TJ filter chain (`packages/crawler/src/adapters/tj-media-direct/filterSteps.ts`)

A 7-step reducer drives `classifyRecord` in `parser.ts`. The `FILTER_STEPS`
array in `filterSteps.ts` is the ONE authoritative statement of the order,
and **the order is load-bearing — do not reorder**:

1. `reviewed-song-drop` — hand-audited TJ-number false positives
   (`reviewedSongOverrides.ts` DROP list) stay out even against stale caches
   or blog rescue.
2. `non-jpn-pro-reject` — an explicit non-JPN `nationalcode` from enrichment
   overrides EVERY admit path, including reviewed-song-allow.
3. `reviewed-song-allow` — hand-audited TJ-number admits (ALLOW list).
   **Classification scope:** K-pop/Korean-artist Japanese releases are admitted ONLY
   by exact TJ number, never artist-wide.
4. `drop-list-reject` — Korean/Chinese artist deny lists, matched against any
   collab component. Strongest negative signal after step 2; **must precede
   `jpn-admit-pro`** — otherwise a drop-listed Korean act carrying a JPN pro
   tag (and not in the ALLOW list) would leak into the corpus. The rule is:
   drop-list wins; genuine K-pop Japanese releases go in the ALLOW list by
   exact TJ number.
5. `jpn-admit-pro` — per-song JPN `nationalcode` (strongest positive signal).
6. `jpn-admit-artist` — lead-component-only artist JPN tag (the primary
   confirmation path by volume). **Its admit branch carries two script
   vetoes** (both return `pass` → fall-through, added 2026-07-13): a
   Korean-script veto (Hangul AND no Japanese over `title + artist`,
   PR #143 — LOCAL regex constants deliberately byte-mirroring the #97
   leakage gate, NOT the broader shared `hasHangul` which also matches
   jamo) and a simplified-Chinese veto (`hasSimplifiedOnlyHan` from
   `@karaoke/search`, PR #148 — deliberately single-sourced with the
   report-only audit). Rationale: the per-song KOR signal is LAGGING (the
   translit pass writes it post-classify), so a newly-surfaced Korean row
   would otherwise leak for exactly one crawl before self-healing;
   Latin/romaji-titled Korean rows carry no script signal, so the curated
   drop lists remain load-bearing for that tail.
7. `blog-rescue` — safety net for TJ-search index gaps. NOT dead code.

Related machinery:

- `reviewedSongOverrides.ts` is TS-only (no JSON sidecar, no Python
  consumer), keyed by leading-zero-normalized TJ number, with structured
  per-entry metadata. Admits emit `via: 'song-override'`.
- `GENERIC_ARTIST_JPN_ADMIT_BLOCKLIST` (`filterSteps.ts`): generic bucket names
  (`variousartists`, `オムニバス`, …) must never blanket-admit on a JPN
  artist tag, and `blog-rescue` refuses to rescue a generic lead component.
- Per-record enrichment uses `searchSongByPro` (exact TJ-number lookup,
  `strType=16` + `strWord='Y'`, client-side exact `pro` match to reject
  neighbor numbers) — not title search. It records the TRUE `nationalcode`
  (incl. KOR/ENG), which feeds `non-jpn-pro-reject`.
- `verdictFromVotes` requires JPN ≥ 3 AND JPN/(JPN+KOR) ≥ 0.7 (symmetric for
  KOR). It works because `bootstrapCharts.ts` sources both JPN votes (JPOP
  chart sweep) and KOR votes (KPOP chart sweep).

## Drop lists, splitter, and sidecar/parity machinery

- Korean drop list: `koreanArtistDropList.ts` (structured
  `DropListEntry { canonical, variants, lastReviewed }`). Its JSON sidecar
  `korean-artist-drop-list.json` is auto-regenerated by
  `scripts/export-drop-list.mjs` on crawler build. The old Python PDF ingest
  consumer was retired; the generated sidecar is still tracked and checked for
  drift after build by both `ci.yml` and `crawl.yml`. Current corpus cleanup
  imports the compiled canonical predicates.
- Chinese drop list: `chineseArtistDropList.ts`, a flat `string[]`
  (schema asymmetry vs Korean is deliberate at its current size; align if it
  grows past ~20). Its former JSON-sidecar/Python machinery was deleted —
  corpus-level cleanup now goes through `scripts/drop-artist-leaks.mjs`
  (`--list korean|chinese`), which imports the canonical predicates straight
  from the built crawler dist, so crawl-time and corpus-level filtering are
  the same predicate by construction. The chinese pass also drops a small
  hardcoded catalog-anomaly ID list (records whose artist field itself is
  malformed, e.g. `tj-72638`).
- `splitArtistCollab` (`packages/crawler/src/clustering.ts`): the ` of `
  member-of-group sub-split fires ONLY inside `(Feat.X)` / `(Prod.X)`
  parentheticals — bare ` of ` never splits (protects BUMP OF CHICKEN).
  `SPLIT_RE_SOURCE` must keep `\s+meets\s+` and the full-width pipe `｜` in
  the delimiter set; both have regression tests
  (`scripts/test_splitter_parity.py` on the Python side). The TS source is
  exported to `packages/crawler/src/clustering-rules.json` at build time so
  Python consumes the sidecar instead of hand-mirroring the regex.
- **Pinned limitation** (documented in
  `packages/crawler/test/clustering.test.ts`): `A(Feat.X)B` with NO delimiter
  around the parenthetical yields a glued `A B` component, not separate
  `A` + `B`. Unobserved in the live corpus; a fix in `splitArtistCollab`
  heals both the parser and `drop-artist-leaks.mjs` at once.

## Merger and alias resolution (`packages/crawler/src/merge.ts`, `aliases.ts`)

- Current order is **A → B → C → D → E → F → reviewed three-way attach → G**.
  A joins shared vendor numbers; B uses normalized title/artist; C uses
  cross-source primary-token matching; D strips guarded context suffixes;
  E/F use reviewed number pairs; G supplies conservative residual rules.
  C remains cross-source-only to avoid same-source original/remix collapse.
- ID/source and disputed-number priority is `tj > tjpdf > joysound > ky > blog`.
  Field ownership is independent: primary title/artist use
  `tj > blog > tjpdf > joysound > ky`; Korean fields use
  `blog > tj > tjpdf > joysound`. Demoting blog IDs does not discard its Korean text.
- Reviewed E/F pairs can attach existing clusters. The full union is checked
  for different values in the same vendor cell; a conflict is logged and
  skipped. The three-way table attaches the other TJ/KY side after E/F, with
  table-to-table invariants. These checks preserve real double registrations.
- `mergeRecords` returns `MergeResult { records, conflicts }` — destructure
  at every call site. Tier C merges are recorded as `tier_c_merge` entries
  but excluded from the headline conflict count (they are merges, not
  disagreements).
- Artist-alias resolution runs BEFORE `mergeRecords`. It splits
  `artist_primary` on **full-width pipe `｜` (U+FF5C) only — never ASCII
  `|`** (ASCII pipe is preserved for band names like `Qverktett:||`). First
  segment becomes canonical, the rest become `artist_aliases`; bare records
  whose `artist_primary` matches a known alias are re-keyed to the canonical.
  Alias collisions (one alias → two canonicals) leave both records untouched
  + warn — never auto-merge across distinct artists. After this resolver,
  Tier B/C cluster naturally on canonical `artist_primary`.

Blog `mintBlogRecordId` selects the first non-null parsed claim in the order
TJ → KY → JOYSOUND, preserving the artist-page ID. A row with all parsed
numbers null emits no record, including a row whose only malformed number
was discarded by parsing. Duplicate minted IDs within one page throw rather
than overwrite another song. Conflict-adjusted cells are unsuitable inputs
for reconstructing those source IDs.

## Schema (`packages/schema/src/index.ts`)

- Cross-field Ajv `allOf if/then` constraint: `title_ko_confidence` is only
  valid when `title_ko_source === 'llm-translated'` — e.g. a blog record
  carrying a confidence tag is schema-invalid.
- `karaoke_numbers.ky` and `.joysound` carry `pattern: '^[0-9]+$'` (digits only).
- TypeBox declarations generate the TypeScript types and Ajv schema from one
  definition. This avoids independent type/schema changes drifting apart.
- Runtime imports need `pnpm -r build` first — `@karaoke/schema` exports
  `dist/index.js`, not `src/index.ts`.
- **The committed corpus is schema-validated on every PR**, not just during
  crawls: `ci.yml`'s `sqlite:build` step imports the committed corpus through
  `@karaoke/data-store`'s `importSongsJson`, which calls `validateSongRecord`
  on each committed record (plus duplicate-id detection) while building the
  self-host SQLite database. Consequence: tightening a schema constraint
  fails PR CI immediately unless the committed corpus already satisfies it —
  you cannot defer enforcement "until the next crawl". Either clean the
  offending committed records in the same PR or land the constraint with a
  regenerated corpus.

## title_ko backfill pipeline

Three optional `SongRecord` fields: `media_context_ko` (Korean rendering of
an anime/OST/OP/ED parenthetical), `title_ko_source`
(`'blog' | 'llm-translated' | 'manual'`), `title_ko_confidence`
(`'high' | 'medium' | 'low'`, llm-translated only — see the schema constraint
above).

- **Stage 1** (`scripts/normalize_tj_title_ko.py`): strips TJ-derived
  katakana→Hangul transliteration `title_ko` from `tj-`/`tjpdf-` records
  (TJ's `sortTitleKo` is a catalog sort helper, not a translation), salvages
  Korean media-context parens into `media_context_ko`, tags blog records
  `title_ko_source = 'blog'`. Idempotent. CRITICAL guard: the TJ branch
  short-circuits when `title_ko_source === 'llm-translated'` already exists —
  without it, re-running Stage 1 would nuke the cached translations.
- **Stage 2** (`scripts/translate_title_ko_via_agents.mjs prep|merge`):
  `prep` chunks eligible records into
  `scripts/data/llm-translations-chunk-NN-input.json`. The output contract and
  prep/merge usage are in [the script guide](../scripts/README.md#title-ko-stage-2).
  `merge` applies the completed outputs and
  (with `--review-csv`) writes the medium/low-confidence review CSV **with a
  UTF-8 BOM** (Korean Excel defaults to CP949 without it → mojibake).
  `filterTranslatableRecords` skips records that already have a
  `title_ko_source` or whose title is pure-Latin, so re-runs only pick up
  newly-crawled records.
- **Cache replay**: the chunk OUTPUT files
  (`scripts/data/llm-translations-chunk-NN.json`) are tracked in git as the
  source-of-truth translation cache; the post-crawl pipeline re-applies them
  on every weekly crawl. `applyDecisionsToCorpus` gates on
  `id + title_primary` match — a TJ title edit invalidates the cached
  translation and the record passes through untouched.
- **Manual fixes sidecar** (`scripts/data/title-ko-manual-fixes.json` +
  `scripts/apply-manual-title-ko-fixes.mjs`): tracked JSON array of
  `{id, title_primary, title_ko}`. Replayed by the pipeline AFTER the Stage 2
  replay, and hard-fails (explicit user corrections gate the crawl, unlike
  the continue-on-error Stage 2 replay). It NFKC-compares `title_primary` to
  guard against stale fixes after a TJ title edit (a mismatched entry is
  skipped, not applied). Applied records get `title_ko_source: 'manual'` and
  DROP `title_ko_confidence`. Adding a fix = edit the sidecar JSON, commit;
  the next pipeline run applies it.
- **Pitfall (2026-07-10, caught in PR #122 review; generalized in PR #125):
  any change that edits a record's `title_primary` (ingest overrides, parser
  fixes, source swaps) silently disarms that record's title_ko on TWO guard
  surfaces** — (1) the manual-fixes sidecar (NFKC guard mismatch → skip with
  exit 0 + stderr counter → an explicit correction quietly reverts) and
  (2) the Stage-2 replay cache (`translate_title_ko_via_agents.mjs` looks up
  by id but APPLIES only when the cached `title_primary` NFKC-matches → the
  cached translation silently stops re-applying). RULE: whenever
  `title_primary` changes for an id present in `title-ko-manual-fixes.json`
  OR the `llm-translations-chunk-*.json` cache, realign BOTH stored
  `title_primary` guards in the SAME change (translations byte-preserved),
  and pin the alignment with tests (patterns: PR #122's manual-fix guard
  test; PR #125's catalog↔manual-fix and catalog↔cache drift pins).

## HTTP client (`packages/crawler/src/http.ts`)

- Default rate limit 200 ms + 50 ms jitter; per-host overrides in
  `HOST_CONFIG` (`www.tjmedia.com` → 500 ms + 100 ms). Per-host `cache`
  opt-out exists; opting out does NOT prune already-cached entries from
  `.cache/http.json`.
- Cache persists are **batched** (default: every 200 stores or 30 s,
  whichever first, plus a final `flush()`): persisting rewrites the entire
  cache file atomically, so persist-per-store is O(n²) IO over a long crawl.
  A crash loses at most the last un-flushed batch — acceptable for a cache.
- The response cache is **unbounded**: every 2xx body is held in one in-memory
  object and the WHOLE object is re-serialized to `.cache/http.json` on each
  persist. A bulk enumeration crossed V8's max string length at ~1.1k large RSC
  pages (`RangeError: Invalid string length` in `JSON.stringify`, 2026-07-10,
  JOYSOUND fullCatalog listing). **Rule:** any one-shot bulk enumeration of
  >~1k large pages must construct the client with `new HttpClient({ cache:
  'off' })` — it never refetches a URL in-run, so the cache buys nothing and is
  pure liability. The weekly-crawl lane leaves the default `'persistent'` mode.
- `searchSong` strips ASCII apostrophes from `searchTxt` to work around a TJ
  server parser bug (`resultCode=04`). Escaping (`\'`, `%27`, …) hits the
  same bug — don't try.
- TJ search cache schema: `{ version, generatedAt, proEnrichmentMap,
  artistNationalityMap }` with a forward-compat `extras` bag that round-trips
  unrecognized top-level fields.

## Data-script conventions

- Corpus-writing helpers use atomic replacement (`<file>.tmp` then rename);
  resumable JSONL collectors have separate append/recovery behavior. Canonical corpus byte
  shape: JSON `indent=2` + trailing newline (Python helper
  `scripts/lib/corpus_io.py:atomic_write_corpus`, JS
  `scripts/lib/corpus.mjs` / `scripts/lib/atomic-write.mjs`).
- `scripts/run-post-crawl-pipeline.mjs` owns the `KARAOKE_SONGS_JSON` env var
  for its child steps: set (absolute) when `--corpus` is given, deleted
  otherwise. Caveat: the cache-prune step always mutates the real
  `tj-search-cache.json` regardless of `--corpus`.
- `tjpdf-*` now comes from `scripts/data/tjpdf-catalog.jsonl` through
  `ingest-tjpdf-catalog.mjs`; PDF parsing and its old text input are retired.
  Catalog `checkedAt` supplies deterministic timestamps, so unchanged catalog
  input does not churn `crawled_at` on every replay.
- `--limit N` has adapter-specific units: blog pages versus yielded records
  in KY/JOYSOUND. `0` means unlimited. A small limit is not a representative
  full catalog, and an explicit network crawl is separate from local tests.

## Search DB: delta patch and query expansion

- `createSongDatabase` converges a legacy DB to the canonical schema, which for
  a fully-derived table it can't ALTER in place (drop `search_texts.text_norm`,
  widen a `field`/`kind` CHECK) means DROP+recreate — a WHOLE-TABLE operation
  that empties that table for EVERY song. The delta patcher calls
  `createSongDatabase` and used to then re-derive only the touched songs, so on
  a pre-#93-shaped served DB (the live v21 case) every UNTOUCHED song silently
  lost its `search_texts` rows (the exact-text serve tier) while docstrings
  claimed the tables were "cleared per-song by the delta patcher". Rule: a
  migration that drops a derived table inside the delta path MUST trigger full
  re-derivation of ALL songs (now implemented — `createSongDatabase` returns a
  `SongDatabaseMigrationReport`, and the patcher falls back to a whole-corpus
  rebuild + full stat recompute + full hint materialization; regression-tested
  in `packages/data-store/test/delta-legacy-migration.test.ts`). Never document
  per-song safety for a whole-table operation.
- `expandSearchQuery` (`@karaoke/search`) feeds user query text to wanakana
  (`toHiragana`/`toKatakana`/`toRomaji`), which parses by per-token recursion
  and overflows the call stack near ~5-6k ASCII chars; unbounded `q` surfaced as
  `/api/search` HTTP 500s. Rule: bound query length at the API edge (the worker
  rejects `q` over 256 code points with a 400) AND guard expansion length inside
  `@karaoke/search` (`MAX_EXPANDABLE_QUERY_CODE_POINTS`, returns the query
  unchanged above the bound). Never feed unbounded user text to wanakana.

## Frontend / UI invariants

- Playwright e2e must use `await page.goto('')` (relative empty string).
  `goto('/')` resolves against the configured base URL's origin and sends the
  test to the bare domain root. Keep the relative-empty form so the same spec
  works for root deployments and any future non-root preview base.
- `apps/web/src/styles/global.css` was hoisted verbatim from
  `index.astro`'s `<style is:global>` — **rule order is load-bearing; do not
  reorder or split** (pinned in the file's first line).
- `--header-height` token (`apps/web/src/styles/tokens.css`) must match the
  actual rendered header: editing `.site-header` padding or
  title/subtitle typography requires re-deriving the value (formula pinned
  in a comment next to the token) or the sticky tab bar leaves a visible
  stripe at scroll. A `ResizeObserver` migration is a pinned TODO.
- Mobile `.tab-bar` edge-bleed needs BOTH `width: calc(100% + …)` AND
  negative `margin-inline`; `--rui-pill-secondary-bg` (light `#dfdfe2`) must
  stay visually distinct from both `--rui-surface` and `--rui-surface-hover`;
  mobile chip sizing intentionally differs from desktop; horizontal chip
  rows hide their scrollbars deliberately. Details and reasons are in
  `tokens.css` / `global.css`; changing them can alter the established mobile layout.
- Tab switch in `App.tsx` deliberately resets ephemeral filter state (input,
  query, vendor chips) and cancels the pending search debounce; favorites
  (localStorage `karaoke-favorites:v1`) persist. The render branch is a typed
  `RenderMode` discriminator with an exhaustive `default: never` arm — adding
  a mode without a case fails typecheck.

## Self-host paths and permissions

The current host is `oci`. The registered API unit uses
`/srv/nas/karaoke/app/apps/worker` as its working directory,
`/srv/nas/karaoke/karaoke-api.env` as its environment file, and
`ReadWritePaths=/srv/nas/karaoke`. This is broader than the serving DB directory.
The env file, TLS material in `tailscale-cert/`, and migration configuration
copies are service dependencies. NAS root `README-ops.md` records the actual
paths; a root directory listing is not a list of disposable development files.

## Windows development notes

- Always `corepack pnpm` — plain `pnpm` may not be on PATH.
- `songs.json` may show a perpetual CRLF-related modified stat flag with zero
  content diff on Windows; `.gitattributes` (`*.json text eol=lf`) handles
  content. Don't stage it in maintenance commits unless the diff has hunks.
- Git over the SMB mount has shown phantom mode/line-ending changes and
  partial checkout/unlink failures. NAS-local Git distinguishes these from
  actual content changes without relying on Windows file-mode reporting.

## CI, test gates, and record identity (2026-07-09/10 incident lessons)

- `pnpm -r test` (pnpm recursive run) BAILS at the first failing package, so
  every later package silently never runs — after #95 the crawler corpus
  regression masked 14 apps/web parity failures for days, and a "one failure"
  summary was really "one failure plus everything behind it unexecuted". Rule:
  any full-workspace test verdict must use `pnpm -r --no-bail test` (or
  per-package runs) and cite per-package summaries; a bailed run proves only
  the packages that actually executed.
- PRs opened with the default `GITHUB_TOKEN` (e.g. by
  peter-evans/create-pull-request in `crawl.yml`) do NOT trigger
  `on: pull_request` workflows — GitHub suppresses recursive workflow events —
  so `ci.yml` never ran on weekly crawl PRs and #95 merged a regressing corpus
  with green-looking status that only turned red on the post-merge main push.
  Rule: any PR a workflow generates must carry its gates INSIDE the generating
  workflow (the crawl job now runs the crawler corpus-regression suite and the
  parity-baseline regen/delta before opening its PR); never assume repo-level
  CI covers bot-opened PRs.
- `blog-*` record ids WERE POSITIONAL (`blog-{artistId}-{rowIndex}`): any crawl
  that re-touched a blog page reassigned the id→song mapping wholesale (#95
  moved 137/149 ids on the Utada page alone). Anything that pinned a blog id
  across crawls silently re-targeted a different song — this broke the parity
  smoke fixtures and device favorites (localStorage stores record ids). The
  stable-identity design SHIPPED 2026-07-14: blog rows now demote to the lowest
  merge rank (merged clusters take the vendor id) and standalone blog rows mint
  `blog-{artistId}-{vendor}-{number}` from the row's first claimed vendor number
  — stable across crawls. Fresh parsing uses that scheme, while the v25
  frozen-data lineage still includes old positional blog IDs. Merge-induced
  vendor-ID changes have already occurred; existing localStorage favorites
  have no migration and can become unresolved.
  Rule (still holds): never pin a positional `blog-{artistId}-{rowIndex}` id
  across crawls; the stable identity is the vendor karaoke number (tj/ky/joysound),
  which the new minting now encodes directly.
- Re-keying id-keyed SIDECARS (Stage-2 title_ko cache, `search-hints.jsonl`) to
  a FUTURE corpus state must model CRAWL-time semantics, not candidate state.
  The post-crawl sweep's conflict resolution nulls misattributed blog vendor
  cells in the built candidate, so minting residual ids from candidate records
  diverges from what the normalizer mints from the parsed blog rows (the v22
  two-sided replay found 297 such rows + 10 title-merged numberless). Rule:
  derive re-key targets from the parsed source numbers — the authoritative
  old→new map the replay emits — never from post-conflict-resolution candidate
  state.

## Script detection: kana punctuation is not script

Context: title language classification (Stage-2 eligibility, "Latin-only →
title_ko null" decisions, any future script predicate).
Pitfall: the katakana middle dot ・ (U+30FB), its half-width form ･ (U+FF65),
and the sound marks ゛゜ (U+309B/309C) sit INSIDE the kana Unicode blocks but
are punctuation/stylization, not script — romanized titles use ・ as a word
separator ("HI・MI・KO", "Merry・Go・Round"). A naive kana-block regex counts
them as Japanese; the inverse mistake (an ASCII-focused check that ignores
kana entirely) mislabeled 22 genuinely-Japanese titles as "pure Latin" with
an unearned high-confidence null title_ko (recovered 2026-07-13, PR #147).
Rule: script predicates must exclude U+30FB/FF65/309B/309C from "contains
kana/Japanese" decisions, and any "Latin-only" verdict must be computed on
the core title with ALL trailing parenthetical groups removed (depth-counting
— tie-up parens nest, e.g. "…(激闘!クラッシュギアＴ(ターボ) OP)").


## July 20 merge and cleanup findings

- Reviewed pairs were previously restricted to singletons, so a valid pair
  stopped applying if an earlier tier had already formed a cluster. E/F now
  resolve vendor numbers across clusters, with full-union number collision
  checks. A confirmed identity alone does not resolve two real numbers in a
  one-number-per-vendor schema.
- `drop-artist-leaks.mjs` once matched 112 v24 rows by exact-ID/artist rules:
  11 actual leaks plus 101 valid Japanese-market releases. Artist-name drops
  now skip JOYSOUND-bearing rows in both Korean and Chinese passes. Explicit
  per-ID anomaly drops still apply. This protects Japanese releases by Korean
  artists without granting artist-wide admission to unrelated songs.
- KY tie-up context is removed with `normalizeKyTitle`, including movie/drama
  suffixes. Version/cut markers are retained: removing `(Short Ver.)` or
  `(Live)` can collapse separate recordings. KY titles remain last in primary
  text ownership because of source truncation risk.
- `merge-ky-into-corpus.mjs` exit **3** reports a failed conservation check.
  A shell chain using `set -e` stops there. A stopped build was once mistaken
  for Tailscale terminating a background process; its report/exit status
  explained the failure instead. Exit 3 is not a successful build verdict.
- Review-file order matters in `encode-b-wave-merge-pairs.mjs`: later files
  explicitly override earlier decisions and log the override. D-series
  supplemental evidence can supersede a B-series uncertain/reject verdict.

## Search and deployment pitfalls

- API mode supports multiple vendors and favorite-record lookup; older docs
  claiming these always use local search were stale. Vendor selection is OR.
- The PWA shell and corpus have different cache lifecycles. `songs.json` is
  loaded lazily on API failure and runtime-cached for seven days. A cold-cache
  device that is already fully offline has no fallback corpus. An app-shell
  update also does not necessarily replace an existing corpus cache immediately.
- Search-only hints are build inputs, not stored display aliases. Full DB
  rebuilds need `--search-hints data/search-hints.jsonl` to preserve their recall.
- Delta `tokenStatMode: 'affected'` does not refresh untouched token IDF when
  total song count changes (112/148 tokens differed in the July audit).
  `tokenStatMode: 'all'` or full import recomputes all statistics. A delta hint
  for an untouched song is warned about but not materialized by the touched-only path.
- `db/current` is a symlink. Moving files under it changes the release itself.
  Promotion/rollback changes the link and restarts the API's DB connection.
- `dbUpdatedAt` is the latest crawl date, not the release date; rebuilt v25
  correctly still reports July 16. Changed records and the release link identify
  a recomposition more reliably than the footer date.
- Wrangler deploys run from `apps/web` so `functions/` accompanies static assets.
  A visible page alone does not establish that `/api/*` is deployed correctly.
- A detail sweep cannot discover records absent from its listing. A stale
  JOYSOUND listing once lost 49,683 numbered rows while raising ruby coverage
  percentage by shrinking the denominator. Compare absolute coverage and
  vendor-number conservation, not percentages alone.


## Reviewed three-way attachments

`REVIEWED_TIER_F_3WAY_ATTACH_PAIRS` adds the second TJ/KY side after ordinary
E/F unions have been applied. Planning both attachments against the same
pre-union snapshot would miss conflicts between those two sides. A conflicting
attachment is skipped while the original owner-to-JOYSOUND pair stays merged;
replacing the pair with an all-or-nothing triple would regress that behavior.

Import-time checks cover the expected 85 entries, unique vendor/number targets
(including exclusion from existing E/F targets), one extra attachment per
JOYSOUND number, existence of its E/F owner, and a different vendor from that
owner. The full cluster union is still checked for vendor-number conflicts.
An owner pair skipped at runtime does not invalidate the independently reviewed
attachment. If a future crawl supplies the shared number directly, Tier A may
make the reviewed attachment a no-op.

The encoder derived 83 KY attachments plus two supplemental cases: KY 41123
to JOYSOUND 11509 (owner TJ 25640), and TJ 26145 to JOYSOUND 1546 (owner KY
40449). The latter replaced an earlier rejection after finding the actual duet;
later review files override earlier verdicts explicitly. Inputs remain in
`scripts/data/b-review-merge-verdicts/`, including C-1 and D-2 files.

Final v25 counts are E 271 + F 482 + attachments 85 = 838 reviewed units,
834 applied and four skipped for real number conflicts (`tj-6579`, `tj-27098`,
`tj-27416`, `tj-26737`). These are different valid registrations under a schema
with one number per vendor; deleting one number is not an identity fix.
The v24→v25 reduction of 152 rows comprises 141 merged rows and 11 removed
leaks. NAS `runs/ky-v23-20260716/` holds the conservation reports.

## JOYSOUND screening and stale decision logs

The June 10 review reduced 175 allowed selection numbers to 172:

| selSongNo | Song / artist | Reason for removal |
|---|---|---|
| 148140 | Super Star / ハン・スンヨン(KARA) | Korean drama OST; Japanese distribution did not prove a Japanese version |
| 153397 | トライアングル / 東方神起 | Korean Tri-Angle recording; Japanese subtitles were mistaken for Japanese lyrics |
| 735357 | ミチGO / G-DRAGON | Korean single; appearance on a Japanese compilation was insufficient |

The running detail sweep had already loaded the older override set. Updating
the classifier source did not change its in-memory decisions or existing log.
`build-joysound-candidate.mjs` therefore removes these exact numbers through
`CHECKPOINT1_EXCLUDED_SEL_SONG_NOS` / `excludeCheckpoint1Admits`. Rebuilding
code alone does not repair recorded decisions consumed without reclassification.

JOYSOUND page/group IDs and machine selection numbers are separate namespaces;
constructing an evidence URL directly from a machine number can identify a
different song. Listing-only classification also omits foreign-name signals
from detail responses (`songNameForeign`, `artistNameForeign`). A correct
review outcome can still contain a wrong album citation. `ive feat.初音ミク`,
for example, is a Japanese Vocaloid producer rather than the K-pop group IVE.

The full-listing output and progress sidecar form one resumable state. A
nonempty output without its sidecar is not safely resumable. Large decision
logs use streaming JSONL rather than one serialized string.

## Classifier replay limits

JOYSOUND uses ordered gates with an import-time phase assertion. July's gate
refactor and shared-predicate alignment were replayed over 352,290 v22 rows
without changed verdicts. Reason-only changes still need record comparison;
a summary that counts only verdicts can conceal them.

Expanded kana handling covers half-width kana and phonetic extensions. Han
and Hangul predicates cover code points beyond the original narrow ranges,
but TJ's script veto deliberately mirrors its narrower leakage check. The
76-character simplified-Han set avoids shared Japanese glyphs (zero hits on
the 313,467-row v22 calibration corpus); it cannot detect every traditional-
script or Latin-titled foreign track. A warm-cache crawl with no observed
leaks did not rule out new-row admission before nationality enrichment.

## KY source and title recovery

The adapter walks
`https://kysing.kr/karaoke-book/?city=jp&s_cd=2&s_page={n}&s_value={letter}`
over 107 values: 80 hiragana readings, `0` for other readings, and A–Z.
It continues to an empty page, deduplicates normalized KY numbers, and treats
`limit` as emitted records. HTTP failures abort; empty title/artist rows are
recorded and skipped, with a parse-error threshold above 1% after at least
50 observed rows. The Japanese index still passes through song overrides,
artist rules, and script filtering.

Detailed pages were truncated like the index: only 1/270 repair requests
succeeded in the July 16 run. The adapter now uses the offline
`curated-title-recovery.json` map (`{ky: {title, artist, source}}`), classifies
recovered rows, and excludes unrecovered truncations. Reasons distinguish
`admit-title-recovered` from `truncation-unrecovered`.

The initial map had 2,076 complete anime-book entries plus KY 44092 Connecting.
Incomplete title/artist cards were excluded, including 445 with missing artist
from the 2,521-card source. Static replay projected recovery of 215/269 excluded
rows and blog parity from 92.26% to 97.13%; these were historical input-specific
measurements, not present-day whole-catalog completeness. KY remains last in
primary text ownership. Shared `normalizeKyNumber` prevents missed number joins;
`normalizeKyTitle` removes tie-up context while preserving Live/Short Ver. cuts.

The July source survey found several dead ends worth retaining: WordPress
REST/sitemaps did not expose the song catalog; artist substring searches mixed
unrelated acts; the `new_ky.asp` date parameter still returned the same latest
100 records and was not a backfill API; manana's KY feed stopped at March 2026
in that observation. The summer anime book supplied useful title/reading/context
data, but its Korean metadata is not automatically imported by this adapter.
These external-source observations are dated, not freshly verified API claims.

## TJ API observations

June's source survey observed a full-catalog `newSongOfMonth` response without
the legacy `searchSong` 30-row cap. Pagination arguments on legacy search
returned the same results; the modern HTML search paginated separately.
`searchSong` uses `strType=16` for exact-number probes and `2` for artists;
the chart endpoint uses a different axis: `strType=3` JPOP, `1` K-pop.
Catalog rows had no authoritative per-song nationality; `searchSong`
`nationalcode` supplied JPN/KOR/ENG, with chart votes as weaker artist evidence.

Observed metadata included `com` (composer), `word` (lyricist), `publishdate`,
`icongubun`, `mv_yn`, thumbnails, and chart rank. Availability in a source
response did not mean inclusion in SongRecord or search. No lyrics/key/tempo
endpoint was found in that survey; this is a historical search result, not a
guarantee about future vendor APIs. The apostrophe parser failure and exact-
number response guard remain documented in the HTTP section above.

## Filter decision logs

TJ `--decisions-out` writes `{tj,title,artist,decision,step,reason}` for catalog
rows that reach classification. Missing number/title/artist rows are not
decisions. `no-admit-path` means no rule admitted a row, not an explicit reject.
Only the final rescue re-parse is written, with overwrite semantics; combining
earlier parses would create contradictory duplicates. Blog seed probes happen
later and have separate statistics, so catalog admit counts can differ from
the adapter's total output.

The Korean/Chinese corpus cleanup logs actual drops and JOYSOUND-anchored
matches spared as `decision: 'skip'`, `reason: 'skipped-joysound-anchored'`.
Ordinary retained rows and reviewed TJ allowances are not separately logged.
Exact anomaly IDs are checked before exemptions. Line count is therefore not
drop count. The current PR composer labels all cleanup reasons as `drop`,
including spared rows; the raw JSONL decision field remains authoritative.

`FILTER_DECISIONS_DIR` separates `tj-filter.jsonl`, `ky-filter.jsonl`,
`drop-kpop-leaks.jsonl`, and `drop-cpop-leaks.jsonl`. Reusing a file path loses
the earlier log. Artifact upload runs with `if: always()`; missing/malformed
report inputs produce notes rather than failing the crawl. The headline TJ
kept/dropped numbers cover catalog parsing only. Blog/KY parity is likewise
report-only; the formerly proposed 95% threshold is not enforced.

## Offline size and parity

The June 10 desktop Node experiment indexed 221,075 records: JSON parsing took
527 ms, MiniSearch construction 5,665 ms, heap about 316 MB, RSS about 485 MB.
The 85.5 MB JSON compressed to 9.6 MB gzip, but transfer size did not predict
browser indexing cost. These are one-machine historical measurements, not
phone benchmarks.

July's full-pack prototype measured 75.1 MiB raw / 21.6 MiB brotli for FTS-only
SQLite, and 99.8 MiB raw / 34.8 MiB brotli for trigram. Neither reproduced
short CJK, choseong, or romaji-prefix semantics. Additional postings were
excluded from those sizes. Server range-query estimates did not establish
browser performance, and iOS OPFS persistence/eviction was not device-verified.
The full-pack direction was retired July 20 in favor of the JSON subset.

The v25 subset update moved mean parity Jaccard from 0.621 to 0.603 with the
same top-one rate; seven query reductions were reviewed as KY-related ranking
changes rather than missing target songs. The v26 update (+262 rows, 224 rows
changed) moved it from 0.603 to 0.601 with the same top-one rate; the four
changed queries kept their top-four results. Snapshot regeneration records a
new baseline; it does not prove that every ranking change is acceptable.

## Translation and supplemental-catalog history

Replacing PDF parsing with exact TJ-number probes retained the `tjpdf-*` IDs:
translation caches, manual corrections, and reviewed pairs depended on them.
Dropping that source would have lost 43 songs and TJ numbers from another 589
in the historical full corpus. Source-title cleanup realigned six manual-fix
guards and 353 cache guards without losing their 376 Korean titles.

Another title-drift study found 169 real translation losses among 380 changed
guards; realigning 298 TJ guards recovered 149 titles. Positional blog IDs and
24 interior-whitespace cases were excluded because loose matching could cross
song identities. July's evidence review left 388 medium/low decisions without
a confirmed canonical Korean title; that is a recorded uncertainty, not an
instruction to upgrade confidence. Decision formats and replay commands are
in [the script guide](../scripts/README.md#title-ko-stage-2).
